Don't try to change the `String word` you start with – create a new String instead.

(Strings are <word data-key="immutable">immutable</word>, and strange things can happen when you try to change them.)